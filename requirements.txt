# Core
openai>=1.40.0          # OpenAI-compatible client (works for Fireworks + self-hosted vLLM)
pydantic>=2.7.0         # data contracts / structured validation
python-dotenv>=1.0.1    # load .env in local dev
streamlit>=1.36.0       # UI
pandas>=2.2.0,<3.0      # avoid pandas 3 / pyarrow 25 Streamlit AppTest segfault
pyarrow>=7.0,<25.0      # pyarrow 25 segfaults in Streamlit dataframe serialization on CI
plotly>=5.22.0          # charts

# Dev
pytest>=8.2.0
ruff>=0.5.0

# Fine-tuning / serving (AMD ROCm pod installs torch from rocm index; see docs/FINE_TUNING.md)
# torch --index-url https://download.pytorch.org/whl/rocm6.2
# transformers
# peft
# accelerate
# vllm
# bitsandbytes-rocm  # optional, only if QLoRA works on the pod image
